/** Single full-width inbox bar (~56px); keep in sync with InboxShellNav min-height */
export const SALES_INBOX_HEADER_STACK_PX = 56;

/** Extra slack for board padding (`p-4` top+bottom ~32px + small gap) */
export const SALES_INBOX_BOARD_VERTICAL_SLACK_PX = 48;
